import { OptionDisplayDirective } from './option-display.directive';

describe('OptionDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
